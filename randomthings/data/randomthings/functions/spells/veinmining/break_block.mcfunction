#Get Loot depending on item of player(temporarily stored in armor stand)
loot spawn ~ ~ ~ mine ~ ~ ~ mainhand

#Play sound Effect(maybe)

#Particles(maybe)

#Destroy Block
setblock ~ ~ ~ minecraft:air replace

#Finally kill @s
kill @s